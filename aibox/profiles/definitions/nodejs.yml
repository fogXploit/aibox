# Node.js Development Profile
# Note: Node.js is pre-installed in the base image for AI CLI support
# This profile provides additional npm configuration and tools

name: nodejs
description: Node.js development with npm/yarn/pnpm support
versions:
  - "20"
  - "22"
  - "24"
default_version: "24"
package_manager: npm

system_dependencies: []

install_commands: []

env_vars:
  NODE_VERSION: "${VERSION}"
  NPM_CONFIG_PREFIX: "/home/aibox/npm-global"
  PATH: "/home/aibox/npm-global/bin:$PATH"

# Node.js is already installed in the base image at the selected version;
# NodeSource packages bundle a current npm, so no extra layers are needed
docker_layers: []

post_install:
  - "npm --version"
  - "node --version"
